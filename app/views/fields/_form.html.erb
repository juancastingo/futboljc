<%= form_for(field) do |f| %>
  <% if field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(field.errors.count, "error") %> prohibited this field from being saved:</h2>

      <ul>
      <% field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nr %>
    <%= f.number_field :nr %>
  </div>

  <div class="field">
    <%= f.label :peopleAmount %>
    <%= f.number_field :peopleAmount %>
  </div>

  <div class="field">
    <%= f.label :precio %>
    <%= f.text_field :precio %>
  </div>

  <div class="field">
    <%= f.label :club_id %>
    <%= f.number_field :club_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
